"""Параметры конфигураций и список seed'ов.

В соответствии с ТЗ сравниваем две конфигурации ГА, отличающиеся
ТОЛЬКО одним фактором (вероятностью мутации p_m), плюс baseline —
случайный поиск при том же бюджете вычислений.
"""

# Базовая конфигурация. Все параметры, кроме явно указанных ниже,
# одинаковы для A и B.
BASE = dict(
    pop_size=100,       # размер популяции
    p_c=0.9,            # вероятность кроссовера
    p_m=0.1,            # вероятность мутации на ген
    sigma=0.1,          # масштаб гауссовской мутации
    tournament_k=3,     # размер турнира при селекции
    elitism=2,          # сколько лучших родителей переходит в новое поколение
    budget=50_000,      # бюджет: число вычислений фитнес-функции
)

CONFIGS = {
    'A': dict(BASE),                     # базовая
    'B': dict(BASE, p_m=0.3),            # изменён только p_m: 0.1 -> 0.3
}

# 20 независимых запусков с разными seed
SEEDS = list(range(1, 21))